int main(int argc, const char * argv[]){
    /*
      There is a 2D array representing image, write a function that will invert pixel values, such that newPixel = 255 - oldPixel.
      Solution should run in multithreaded mod if width * height is more than 5000.
     
      void invert_image(uint8_t **pixels, size_t width, size_t height);
    */
    
    
    return 0;
}